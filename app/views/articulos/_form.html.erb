<%= bootstrap_form_for(@articulo) do |f| %>
  <% if @articulo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@articulo.errors.count, "error") %> prohibited this articulo from being saved:</h2>

      <ul>
      <% @articulo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.text_area :contenido %>
  </div>
  <div class="field">
   <%= f.file_field :foto %>
 </div>
  <div class="actions">
    <%= f.submit 'Actualizar', :class => 'btn btn-success' %>
    <%= link_to 'Atrás', '/articulos', :class => 'btn btn-danger' %>
  </div>
<% end %>
